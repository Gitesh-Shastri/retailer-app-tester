<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context=".Medicine_List">
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/Base.TextAppearance.AppCompat.Medium"
        android:textColor="@color/colorPrimary"
        android:id="@+id/list_text"
        android:text="Please provide your exhaustive medicine list so that we provide 100% fulfillment to you at minimal time."/>
    <AutoCompleteTextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/list_text"
        android:hint=" Enter Your Medicine "
        android:id="@+id/medicine_edit_tv3"
        android:completionThreshold="1"
        android:inputType="textPhonetic"
        />
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/filter1"
        android:layout_marginTop="10dp"
        android:layout_above="@+id/submit"
        android:layout_marginBottom="10dp"
        android:id="@+id/order_list_scroll1">
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/list_make"
        />
    </ScrollView>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@+id/Continue"
        android:layout_alignParentBottom="true"
        android:layout_marginRight="10dp"
        android:id="@+id/submit"
        android:text="Submit"
        android:background="@drawable/border_curved"
        android:textColor="#fff"
        />
    <Button
        android:layout_width="wrap_content"
        android:id="@+id/Continue"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="Continue Shopping"
        android:textColor="#fff"
        android:background="@drawable/border_curved"
        />
</RelativeLayout>